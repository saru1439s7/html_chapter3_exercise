
public class kasan {
	public static kasan(String[] args) {
		int kansan;
//		メソッド「kasan」は、引数で受け取った配列の値を足した合計値(和)を返す。
//		ただし、値が 2 未満の数値は加えず、値が 2 以上の場合のみ合計値に加える。

	int kansan(int a) {
		if (2 <= a) {
			kansan = ++a;
			return kansan;
		}
	}

}
